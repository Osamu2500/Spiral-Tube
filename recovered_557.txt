{
  "AllowMultiple": "false",
  "Description": "\"Fixed issue where clicking checkboxes or video cards in multi-select mode would still play the video due to YouTube's early event capture.\"",
  "EndLine": "404",
  "Instruction": "\"Update click interception logic to capture all pointer events and prevent YouTube from playing the video when selecting.\"",
  "ReplacementContent": "\"    _createAndAppendCheckbox(card, videoId, href, title) {\\n        const cb = document.createElement('div');\\n        cb.className = 'ypp-ms-checkbox';\\n        cb.innerHTML = ICONS.CHECK;\\n\\n        // Try classic thumbnail containers first\\n        let thumb = card.querySelector('ytd-thumbnail, #thumbnail, yt-image-view-model');\\n        \\n        // Fallback: use the anchor tag itself if container not found\\n        if (!thumb) {\\n            thumb = card.querySelector(`a[href*=\\\"${videoId}\\\"]`);\\n        }\\n\\n        if (thumb) {\\n            // Ensure the parent container can anchor absolute positioned elements securely\\n            if (window.getComputedStyle(thumb).position === 'static') {\\n                thumb.style.position = 'relative';\\n            }\\n            thumb.appendChild(cb);\\n        }\\n        \\n        // Removed separate cb listener. Click logic is fully handled by _bindCardClickInterception.\\n    }\\n\\n    _bindCardClickInterception(card, videoId, href, title) {\\n        const interceptor = (e) => {\\n            const isCheckbox = !!e.target.closest('.ypp-ms-checkbox');\\n            const shouldIntercept = this.controller.isSelectionModeActive() || e.shiftKey || isCheckbox;\\n\\n            if (shouldIntercept) {\\n                // Stop YouTube's SPA router from seeing this event early\\n                e.stopPropagation();\\n                e.stopImmediatePropagation();\\n                \\n                // Prevent browser navigation, but avoid preventing pointerdown \\n                // to keep touch scrolling functional on touchscreens.\\n                if (e.type === 'click' || e.type === 'mouseup') {\\n                    e.preventDefault();\\n                }\\n\\n                // Execute the actual selection logic only once (on click)\\n                if (e.type === 'click') {\\n                    this.controller.toggleSelect(videoId, href, title);\\n                }\\n            }\\n        };\\n\\n        // Use capture phase to intercept events BEFORE YouTube's framework gets them.\\\n<truncated 472 bytes>",
  "StartLine": "351",
  "TargetContent": "\"    _createAndAppendCheckbox(card, videoId, href, title) {\\n        const cb = document.createElement('div');\\n        cb.className = 'ypp-ms-checkbox';\\n        cb.innerHTML = ICONS.CHECK;\\n\\n        // Try classic thumbnail containers first\\n        let thumb = card.querySelector('ytd-thumbnail, #thumbnail, yt-image-view-model');\\n        \\n        // Fallback: use the anchor tag itself if container not found\\n        if (!thumb) {\\n            thumb = card.querySelector(`a[href*=\\\"${videoId}\\\"]`);\\n        }\\n\\n        if (thumb) {\\n            // Ensure the parent container can anchor absolute positioned elements securely\\n            if (window.getComputedStyle(thumb).position === 'static') {\\n                thumb.style.position = 'relative';\\n            }\\n            thumb.appendChild(cb);\\n        }\\n\\n        this.controller.addListener(cb, 'click', (e) => {\\n            e.preventDefault();\\n            e.stopPropagation();\\n            this.controller.toggleSelect(videoId, href, title);\\n        });\\n    }\\n\\n    _bindCardClickInterception(card, videoId, href, title) {\\n        this.controller.addListener(card, 'click', (e) => {\\n            if (e.target.closest('.ypp-ms-checkbox')) return;\\n\\n            if (e.shiftKey || this.controller.isSelectionModeActive()) {\\n                e.preventDefault();\\n                e.stopPropagation();\\n                this.controller.toggleSelect(videoId, href, title);\\n            }\\n        }, true);\\n    }\"",
  "TargetFile": "\"f:\\\\Youtube 2.0\\\\src\\\\content\\\\features\\\\global\\\\behavior\\\\multi-select.js\"",
  "toolAction": "\"Fixing click interception\"",
  "toolSummary": "\"Fix click interception\""
}