import { ICONS, P } from '../ui/popup-icons.js';

export const getPopup_designTab = (t) => ({
    id: 'popup_design',
    label: 'Popup Design',
    icon: ICONS.uiComponents,
    custom: true,
    sections: [],
  });
