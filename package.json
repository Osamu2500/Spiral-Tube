{
  "name": "spiral-tube",
  "version": "2.3.0",
  "description": "Spiral Tube — YouTube Power Extension",
  "scripts": {
    "build:css": "node scripts/build-themes.js",
    "build": "npm run build:css && vite build && vite build --config vite.config.external.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "terser": "^5.49.0",
    "typescript": "^5.5.4",
    "vite": "^5.0.0",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "animejs": "^3.2.2"
  }
}
