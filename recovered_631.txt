{
  "AllowMultiple": "false",
  "Description": "\"Automatically activate selection mode when a video is selected via checkbox, and intercept clicks if there's any selection.\"",
  "EndLine": "521",
  "Instruction": "\"Update _globalInterceptor and toggleSelect to automatically handle selection mode.\"",
  "ReplacementContent": "\"    _globalInterceptor(e) {\\n        // Find if the target is within a video card\\n        const el = e.target instanceof Element ? e.target : e.target.parentElement;\\n        if (!el) return;\\n\\n        const isCb = !!el.closest('.ypp-ms-checkbox');\\n        const isCard = el.closest(this.domManager.selectors);\\n        \\n        if (!isCard) return;\\n\\n        const hasSelection = this.selected.size > 0;\\n\\n        if (isCb || this.selectionModeActive || hasSelection || e.shiftKey) {\\n            // Stop YouTube's SPA router from seeing this event early\\n            e.stopPropagation();\\n            e.stopImmediatePropagation();\\n            \\n            // Prevent browser navigation, but avoid preventing pointerdown \\n            // to keep touch scrolling functional on touchscreens.\\n            if (e.type === 'click' || e.type === 'mouseup') {\\n                e.preventDefault();\\n            }\\n\\n            // Execute the actual selection logic only once (on click)\\n            if (e.type === 'click') {\\n                const { videoId, href, title } = this.domManager._getVideoData(isCard);\\n                if (videoId) {\\n                    this.toggleSelect(videoId, href, title);\\n                }\\n            }\\n        }\\n    }\\n\\n    _onPageChange() {\\n        this._debouncedProcessAll();\\n        this.clearAll(); \\n    }\\n\\n    toggleSelect(videoId, href, title) {\\n        if (this.selected.has(videoId)) {\\n            this.selected.delete(videoId);\\n            this.ui.toggleCardSelection(videoId, false);\\n        } else {\\n            this.selected.set(videoId, { title, href, videoId });\\n            this.ui.toggleCardSelection(videoId, true);\\n        }\\n\\n        // If the user manually clicked a checkbox and we weren't in mode yet, turn it on\\n        if (this.selected.size > 0 && !this.selectionModeActive) {\\n            this.selectionModeActive = true;\\n            this.ui.setGlobalVisibility(true);\\n        }\\n\\n        // If no videos left, we don't automatically turn off mode unless the\n<truncated 150 bytes>",
  "StartLine": "487",
  "TargetContent": "\"    _globalInterceptor(e) {\\n        // Find if the target is within a video card\\n        const el = e.target instanceof Element ? e.target : e.target.parentElement;\\n        if (!el) return;\\n\\n        const isCb = !!el.closest('.ypp-ms-checkbox');\\n        const isCard = el.closest(this.domManager.selectors);\\n        \\n        if (!isCard) return;\\n\\n        if (isCb || this.selectionModeActive || e.shiftKey) {\\n            // Stop YouTube's SPA router from seeing this event early\\n            e.stopPropagation();\\n            e.stopImmediatePropagation();\\n            \\n            // Prevent browser navigation, but avoid preventing pointerdown \\n            // to keep touch scrolling functional on touchscreens.\\n            if (e.type === 'click' || e.type === 'mouseup') {\\n                e.preventDefault();\\n            }\\n\\n            // Execute the actual selection logic only once (on click)\\n            if (e.type === 'click') {\\n                const { videoId, href, title } = this.domManager._getVideoData(isCard);\\n                if (videoId) {\\n                    this.toggleSelect(videoId, href, title);\\n                }\\n            }\\n        }\\n    }\\n\\n    _onPageChange() {\\n        this._debouncedProcessAll();\\n        this.clearAll(); \\n    }\\n\\n    toggleSelect(videoId, href, title) {\\n        if (this.selected.has(videoId)) {\\n            this.selected.delete(videoId);\\n            this.ui.toggleCardSelection(videoId, false);\\n        } else {\\n            this.selected.set(videoId, { title, href, videoId });\\n            this.ui.toggleCardSelection(videoId, true);\\n        }\\n\\n        this.ui.updateActionBar(this.selected.size, this.selectionModeActive);\\n    }\"",
  "TargetFile": "\"f:\\\\Youtube 2.0\\\\src\\\\content\\\\features\\\\global\\\\behavior\\\\multi-select.js\"",
  "toolAction": "\"Update toggle logic\"",
  "toolSummary": "\"Update toggle logic\""
}