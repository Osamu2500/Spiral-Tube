/* ============================================================
   FRUTIGER AERO UI THEME — Web 2.0 / Windows Vista Era Aesthetic
   Inspired by: Windows Vista Aero, Mac OS X Aqua, Windows Media Center,
                Nokia S60, Sony Ericsson W-series, iTunes 7, Windows Live
   Era: 2005–2010 · Mood: Glossy · Sky-Blue · Optimistic · Nature-meets-Tech
   ============================================================ */

/* ============================================================
   GLOBAL ANIMATIONS
   ============================================================ */

@keyframes aeroGlossShimmer {
  0%   { background-position: -200% center; }
  100% { background-position: 200% center; }
}

@keyframes aeroBokehFloat {
  0%   { transform: translateY(0px) scale(1);   opacity: 0.35; }
  33%  { transform: translateY(-18px) scale(1.08); opacity: 0.55; }
  66%  { transform: translateY(-8px) scale(0.95);  opacity: 0.42; }
  100% { transform: translateY(0px) scale(1);   opacity: 0.35; }
}

@keyframes aeroAquaPulse {
  0%   { box-shadow: 0 0 12px rgba(53,199,240,0.35), 0 0 28px rgba(53,199,240,0.15), inset 0 0 20px rgba(255,255,255,0.12); }
  50%  { box-shadow: 0 0 22px rgba(11,134,194,0.50), 0 0 48px rgba(11,134,194,0.22), inset 0 0 30px rgba(255,255,255,0.18); }
  100% { box-shadow: 0 0 12px rgba(53,199,240,0.35), 0 0 28px rgba(53,199,240,0.15), inset 0 0 20px rgba(255,255,255,0.12); }
}

@keyframes aeroFloat {
  0%   { transform: translateY(0px); }
  50%  { transform: translateY(-3px); }
  100% { transform: translateY(0px); }
}

@keyframes aeroBreathe {
  0%   { box-shadow: 0 0 15px rgba(53,199,240,0.10); }
  50%  { box-shadow: 0 0 30px rgba(18,168,221,0.28); }
  100% { box-shadow: 0 0 15px rgba(53,199,240,0.10); }
}

@keyframes aeroAmbientFloat {
  0%   { transform: translateY(0px) rotate(0deg); }
  50%  { transform: translateY(-5px) rotate(0.4deg); }
  100% { transform: translateY(0px) rotate(0deg); }
}

/* ============================================================
   DESIGN TOKENS — Frutiger Aero Color System
   ============================================================ */
html, [dark],
html:not([dark]) {

  /* Sky palette */
  --fa-sky-top:      #bfe6ff;
  --fa-sky-bottom:   #eafcff;

  /* Aqua / Chrome-blue */
  --fa-aqua-400:     #35c7f0;
  --fa-aqua-500:     #12a8dd;
  --fa-aqua-600:     #0b86c2;

  /* Nature green accent */
  --fa-lime-400:     #b6e84a;
  --fa-lime-500:     #8fd12a;

  /* Glass / frost surface */
  --fa-glass-white:  rgba(255, 255, 255, 0.55);
  --fa-glass-border: rgba(255, 255, 255, 0.78);
  --fa-glass-border-bottom: rgba(53, 199, 240, 0.20);

  /* Chrome metallic gradient */
  --fa-chrome-grad: linear-gradient(
    to bottom,
    rgba(255,255,255,0.90) 0%,
    rgba(210,240,255,0.88) 30%,
    rgba(170,220,255,0.85) 60%,
    rgba(190,230,255,0.90) 100%
  );

  /* Subscribe / CTA red-orange */
  --fa-red-orange:   #f03a17;

  /* Glossy specular top-reflection */
  --fa-gloss: linear-gradient(
    to bottom,
    rgba(255, 255, 255, 0.72) 0%,
    rgba(255, 255, 255, 0.28) 48%,
    transparent 50%
  );

  /* Shadows — soft, light, sky-tinted */
  --fa-shadow-base:  0px 4px 20px rgba(18,168,221,0.14), 0px 1px 4px rgba(0,0,0,0.06);
  --fa-shadow-hover: 0px 8px 32px rgba(18,168,221,0.28), 0px 2px 8px rgba(53,199,240,0.14);
  --fa-shadow-active: 0px 2px 8px rgba(18,168,221,0.18), inset 0 1px 3px rgba(255,255,255,0.72);

  /* Timing */
  --fa-bounce:     0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  --fa-bounce-alt: 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.1);

  /* Structure */
  --fa-radius-sm:   8px;
  --fa-radius-md:   12px;
  --fa-radius-lg:   16px;
  --fa-radius-xl:   24px;
  --fa-radius-pill: 500px;
}

/* ============================================================
   BODY & BACKGROUND — Sky gradient + bokeh orbs
   ============================================================ */
html, [dark] body,
html:not([dark]) body {
  background-color: #bfe6ff !important;
}

/* Background Image Layer with Blur */
body::after {
  content: '';
  position: fixed;
  inset: -15px; /* Prevent blur edge bleeding */
  z-index: -2;
  pointer-events: none;
  background-image:
    /* Bokeh orbs */
    radial-gradient(ellipse 280px 280px at 8% 15%, rgba(255,255,255,0.60) 0%, transparent 70%),
    radial-gradient(ellipse 200px 200px at 90% 25%, rgba(182,232,74,0.30) 0%, transparent 65%),
    radial-gradient(ellipse 340px 340px at 50% 5%,  rgba(255,255,255,0.40) 0%, transparent 60%),
    radial-gradient(ellipse 180px 180px at 75% 80%, rgba(53,199,240,0.35) 0%, transparent 65%),
    radial-gradient(ellipse 220px 220px at 15% 75%, rgba(255,255,255,0.45) 0%, transparent 60%),
    radial-gradient(ellipse 150px 150px at 40% 60%, rgba(182,232,74,0.20) 0%, transparent 60%),
    /* Main sky gradient tint overlay */
    linear-gradient(180deg, rgba(191, 230, 255, 0.3) 0%, rgba(216, 242, 255, 0.1) 40%, rgba(234, 252, 255, 0) 100%),
    /* User Background Image */
    url('../../../assets/frutiger-aero-bg.jpg') !important;
  background-size: cover !important;
  background-position: center !important;
  filter: blur(6px) saturate(110%);
}

/* Decorative bokeh pseudo-overlay */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 0;
  background-image:
    radial-gradient(circle 60px at 12% 20%, rgba(255,255,255,0.45) 0%, transparent 100%),
    radial-gradient(circle 40px at 88% 12%, rgba(182,232,74,0.25) 0%, transparent 100%),
    radial-gradient(circle 90px at 55% 8%,  rgba(255,255,255,0.30) 0%, transparent 100%),
    radial-gradient(circle 50px at 78% 75%, rgba(53,199,240,0.28) 0%, transparent 100%),
    radial-gradient(circle 70px at 18% 80%, rgba(255,255,255,0.38) 0%, transparent 100%),
    radial-gradient(circle 30px at 35% 55%, rgba(182,232,74,0.18) 0%, transparent 100%),
    radial-gradient(circle 45px at 65% 45%, rgba(53,199,240,0.20) 0%, transparent 100%),
    radial-gradient(circle 25px at 92% 50%, rgba(255,255,255,0.32) 0%, transparent 100%);
  animation: aeroAmbientFloat 14s infinite alternate ease-in-out;
}

/* Bokeh overlay via #page-manager for deeper coverage */
#page-manager::before {
  content: '';
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: -1;
  background-image:
    radial-gradient(circle 2px at 22% 82%, rgba(255,255,255,0.8) 0%, transparent 100%),
    radial-gradient(circle 3px at 78% 18%, rgba(53,199,240,0.45) 0%, transparent 100%);
  animation: aeroAmbientFloat 16s infinite alternate ease-in-out;
}

/* ============================================================
   FONT SYSTEM — Poppins / Frutiger-family rounded humanist
   ============================================================ */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap');

* {
  font-family: "Poppins", "Segoe UI", "Trebuchet MS", "Verdana", "Lucida Grande", sans-serif !important;
  -webkit-tap-highlight-color: transparent;
  touch-action: manipulation;
}

/* Remove YouTube's touch feedback shapes */
.yt-spec-button-shape-next--mono .yt-spec-touch-feedback-shape__fill,
.yt-spec-button-shape-next--mono .yt-spec-touch-feedback-shape__stroke {
  display: none !important;
}

/* ============================================================
   YOUTUBE CSS VARIABLE OVERRIDES
   ============================================================ */
html, [dark],
html:not([dark]) {
  --yt-spec-base-background: none !important;
  --yt-spec-brand-background-primary: rgba(191,230,255,0.95) !important;
  --yt-spec-brand-background-secondary: rgba(210,240,255,0.90) !important;
  --yt-spec-general-background-a: rgba(220,244,255,0.88) !important;
  --yt-spec-general-background-b: rgba(200,236,255,0.85) !important;

  /* Force Frutiger Aero navy/blue text colors */
  --yt-spec-text-primary: #0a2a4a !important;
  --yt-spec-text-secondary: #0b5fa3 !important;
  --yt-spec-text-disabled: rgba(10, 42, 74, 0.4) !important;
  --yt-spec-text-primary-inverse: #ffffff !important;
  --yt-spec-text-secondary-inverse: rgba(255, 255, 255, 0.8) !important;

  --yt-spec-call-to-action: #0b86c2 !important;
  --yt-spec-call-to-action-inverse: #ffffff !important;

  --yt-spec-badge-chip-background: rgba(190,228,255,0.80) !important;
  --yt-spec-outline: rgba(18,168,221,0.22) !important;
  --yt-spec-filled-button-text: #ffffff !important;
  --yt-spec-mono-tonal-button-bg: rgba(190,230,255,0.85) !important;

  --yt-spec-call-to-action-overlay: rgba(10,42,74,0.10) !important;
  --yt-spec-brand-link-text: #0b86c2 !important;
}

/* ============================================================
   TEXT STYLING & GLOSSY EMBOSS EFFECTS
   ============================================================ */
/* Give video titles and primary texts a subtle bright inset look */
#video-title, 
h1, h2, h3, 
.ytd-video-primary-info-renderer .title,
yt-formatted-string.ytd-video-primary-info-renderer {
  text-shadow: 0 1px 0 rgba(255, 255, 255, 0.8) !important;
}

/* Hover effects for video titles — aqua glow */
#video-title:hover,
a.yt-simple-endpoint.ytd-video-renderer:hover #video-title,
a.yt-simple-endpoint.ytd-grid-video-renderer:hover #video-title {
  color: #12a8dd !important;
  text-shadow: 0 0 12px rgba(53, 199, 240, 0.6), 0 1px 0 rgba(255, 255, 255, 0.8) !important;
}

/* Channel links */
#channel-name .yt-simple-endpoint,
ytd-channel-name .yt-simple-endpoint {
  text-shadow: 0 1px 0 rgba(255, 255, 255, 0.6) !important;
  transition: all 0.2s ease !important;
}
#channel-name .yt-simple-endpoint:hover,
ytd-channel-name .yt-simple-endpoint:hover {
  color: #0b86c2 !important;
  text-shadow: 0 0 8px rgba(18, 168, 221, 0.4), 0 1px 0 rgba(255, 255, 255, 0.8) !important;
}

/* ============================================================
   PAGE SKELETON — Transparent containers
   ============================================================ */
ytd-app,
ytd-masthead,
#page-manager.ytd-app,
#content.ytd-app {
  background: transparent !important;
}

#frosted-glass.with-chipbar.ytd-app {
  background: none !important;
}

/* ============================================================
   NAVBAR / MASTHEAD — Chrome Aero glass bar
   ============================================================ */
#masthead-container {
  background: transparent !important;
  background-color: transparent !important;
}

ytd-masthead #container {
  /* Chrome bar with Vista-era metallic gradient */
  border-radius: var(--fa-radius-pill) !important;

  background:
    var(--fa-gloss),
    linear-gradient(
      to bottom,
      rgba(220, 244, 255, 0.97) 0%,
      rgba(185, 228, 252, 0.94) 50%,
      rgba(160, 218, 250, 0.92) 100%
    ) !important;

  border: 1px solid var(--fa-glass-border) !important;
  border-bottom-color: rgba(18,168,221,0.22) !important;

  box-shadow:
    var(--fa-shadow-base),
    inset 0 1px 0 rgba(255,255,255,0.90) !important;

  margin: 6px 8px 4px !important;

  outline: 1px solid rgba(255,255,255,0.55) !important;
  outline-offset: -2px !important;

  transition:
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

/* Masthead glowing base */
ytd-masthead#masthead {
  box-shadow:
    0 2px 16px rgba(18,168,221,0.18),
    0 4px 32px rgba(18,168,221,0.10) !important;
}

/* ============================================================
   SEARCH BAR — Frosted aqua pill
   ============================================================ */
.ytSearchboxComponentInputBoxDark,
.ytSearchboxComponentInputBox {
  background:
    var(--fa-gloss),
    rgba(225, 246, 255, 0.95) !important;
  border: 1px solid var(--fa-glass-border) !important;
  border-bottom-color: rgba(18,168,221,0.22) !important;
  border-right-color: rgba(18,168,221,0.15) !important;
  border-radius: var(--fa-radius-pill) !important;
  box-shadow: var(--fa-shadow-base), inset 0 1px 3px rgba(255,255,255,0.85) !important;
  transition:
    box-shadow var(--fa-bounce),
    border-color var(--fa-bounce) !important;
}

.ytSearchboxComponentInputBoxDark:focus-within,
.ytSearchboxComponentInputBox:focus-within {
  border-color: rgba(18,168,221,0.55) !important;
  box-shadow:
    var(--fa-shadow-hover),
    0 0 0 3px rgba(18,168,221,0.14),
    inset 0 1px 3px rgba(255,255,255,0.85) !important;
}

.ytSearchboxComponentInput {
  color: #0a2a4a !important;
}

.ytSearchboxComponentSearchButton {
  margin-left: 8px !important;
  border-radius: var(--fa-radius-pill) !important;
  border: 1px solid var(--fa-glass-border) !important;
  background:
    var(--fa-gloss),
    linear-gradient(to bottom, rgba(200,238,255,0.95) 0%, rgba(160,220,252,0.90) 100%) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

.ytSearchboxComponentSearchButton:hover {
  background:
    var(--fa-gloss),
    linear-gradient(to bottom, rgba(175,232,255,0.98) 0%, rgba(130,210,248,0.95) 100%) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-1px) scale(1.04);
}

.ytSearchboxComponentSearchButton:active {
  transform: translateY(0px) scale(0.97);
  box-shadow: var(--fa-shadow-active) !important;
}

.ytSearchboxComponentDesktop .ytSearchboxComponentClearButton {
  margin-right: 10px;
  width: 30px !important;
  height: 30px !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

.ytSearchboxComponentDesktop .ytSearchboxComponentClearButton:hover {
  transform: scale(1.1) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

/* Search box focused glow */
#search.ytd-searchbox:focus-within,
ytd-searchbox:focus-within {
  box-shadow:
    0 0 0 2px rgba(18,168,221,0.42),
    0 0 20px rgba(18,168,221,0.20) !important;
}

/* Search icon */
#search-icon-legacy.ytd-searchbox,
.ytSearchboxComponentSearchButton {
  box-shadow: 0 0 12px rgba(53,199,240,0.30) !important;
}

/* Search suggestions dropdown */
.ytSearchboxComponentSuggestionsContainer,
ytd-multi-page-menu-renderer {
  top: 55px !important;
  border: 1px solid var(--fa-glass-border) !important;
  background:
    var(--fa-gloss),
    rgba(225, 246, 255, 0.97) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  border-radius: 24px !important;
  backdrop-filter: blur(14px) !important;
  -webkit-backdrop-filter: blur(14px) !important;
}

.ytSuggestionComponentRoundedSuggestion {
  margin: 5px 10px;
  border: 1px solid rgba(18,168,221,0.10) !important;
  border-radius: var(--fa-radius-sm) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

.ytSuggestionComponentRoundedSuggestion:hover {
  transform: translateY(-1px) scale(1.01) !important;
  background: rgba(175,232,255,0.50) !important;
  box-shadow: var(--fa-shadow-base) !important;
}

/* ============================================================
   SIDEBAR / GUIDE — Floating frosted sky-blue glass panel
   ============================================================ */
#guide-content.ytd-app {
  background: transparent;
}

#guide-wrapper.ytd-app {
  background:
    var(--fa-gloss),
    rgba(210, 240, 255, 0.90) !important;
  border-radius: 24px !important;
  border: 1px solid var(--fa-glass-border) !important;
  border-bottom-color: rgba(18,168,221,0.18) !important;
  box-shadow: var(--fa-shadow-base) !important;
  backdrop-filter: blur(10px) !important;
  -webkit-backdrop-filter: blur(10px) !important;
  transition:
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

ytd-app[frosted-glass-exp] tp-yt-app-drawer.ytd-app[persistent] {
  top: 75px !important;
  left: 8px !important;
}

tp-yt-app-drawer.ytd-app #contentContainer.tp-yt-app-drawer {
  background: transparent !important;
}

#sections.ytd-guide-renderer > *.ytd-guide-renderer:first-child {
  padding: 10px 3px !important;
}

ytd-guide-entry-renderer {
  border-radius: var(--fa-radius-md) !important;
}

/* Guide entries */
#endpoint.yt-simple-endpoint.ytd-guide-entry-renderer {
  background: transparent !important;
  box-shadow: none !important;
  border-radius: var(--fa-radius-md) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce),
    border-radius var(--fa-bounce) !important;
}

#endpoint.yt-simple-endpoint.ytd-guide-entry-renderer:hover,
#endpoint.yt-simple-endpoint.ytd-guide-entry-renderer:focus {
  background:
    var(--fa-gloss),
    rgba(170,228,255,0.75) !important;
  border-radius: var(--fa-radius-md) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  border: 1px solid rgba(18,168,221,0.22) !important;
  transform: translateX(2px) !important;
}

ytd-guide-entry-renderer:hover #endpoint.yt-simple-endpoint {
  box-shadow:
    0 0 12px rgba(18,168,221,0.22),
    inset 0 0 10px rgba(255,255,255,0.55) !important;
}

#endpoint.yt-simple-endpoint.ytd-guide-entry-renderer:active {
  transform: scale(0.98) !important;
  box-shadow: var(--fa-shadow-active) !important;
}

/* Mini guide */
ytd-mini-guide-renderer.ytd-app {
  top: 70px !important;
  left: 10px !important;
}

ytd-mini-guide-entry-renderer {
  margin-top: 10px !important;
  border-radius: 20px !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

ytd-mini-guide-entry-renderer:hover {
  transform: translateY(-2px) scale(1.06) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  background: rgba(170,228,255,0.72) !important;
}

/* ============================================================
   BUTTONS — Glossy Aqua gel buttons (Windows Vista Aero style)
   ============================================================ */

/* Icon buttons in masthead */
.yt-spec-button-shape-next--overlay.yt-spec-button-shape-next--text,
.yt-spec-button-shape-next--overlay.yt-spec-button-shape-next--tonal,
ytd-notification-topbar-button-renderer.ytd-masthead,
yt-icon-button.ytd-masthead,
.yt-spec-button-shape-next--mono.yt-spec-button-shape-next--text,
ytd-comment-view-model[optimal-reading-width-comments] #action-menu.ytd-comment-view-model {
  border-radius: var(--fa-radius-pill) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

.yt-spec-button-shape-next--overlay.yt-spec-button-shape-next--text:hover,
.yt-spec-button-shape-next--overlay.yt-spec-button-shape-next--tonal:hover,
ytd-notification-topbar-button-renderer.ytd-masthead:hover,
yt-icon-button.ytd-masthead:hover,
.yt-spec-button-shape-next--mono.yt-spec-button-shape-next--text:hover,
ytd-comment-view-model[optimal-reading-width-comments] #action-menu.ytd-comment-view-model:hover {
  background: rgba(170,228,255,0.62) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-1px) scale(1.06);
}

/* Voice search, notification, topbar buttons */
#voice-search-button.ytd-masthead,
.ypp-nav-btn,
ytd-topbar-menu-button-renderer.ytd-masthead,
#trailing-button.ytd-playlist-panel-renderer,
ytd-menu-renderer .ytd-menu-renderer[style-target="button"],
.yt-spec-button-shape-next--mono.yt-spec-button-shape-next--tonal {
  border-radius: var(--fa-radius-pill) !important;
  background-color: transparent !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

#voice-search-button.ytd-masthead:hover,
.ypp-nav-btn:hover,
ytd-topbar-menu-button-renderer.ytd-masthead:hover,
#trailing-button.ytd-playlist-panel-renderer:hover,
ytd-menu-renderer .ytd-menu-renderer[style-target="button"]:hover,
.yt-spec-button-shape-next--mono.yt-spec-button-shape-next--tonal:hover {
  background:
    var(--fa-gloss),
    rgba(170,228,255,0.68) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-1px) scale(1.06);
}

/* Nav button active */
.ypp-nav-btn.active {
  background: rgba(18,168,221,0.18) !important;
  box-shadow:
    0 0 14px rgba(18,168,221,0.48),
    0 0 28px rgba(53,199,240,0.22),
    var(--fa-shadow-active) !important;
  border: 1px solid rgba(18,168,221,0.45) !important;
}

/* Tonal buttons: Share / Ask / Save / Download */
button.ytSpecButtonShapeNextTonal:not(.ytSpecButtonShapeNextSegmentedStart):not(.ytSpecButtonShapeNextSegmentedEnd) {
  border-radius: var(--fa-radius-pill) !important;
  background:
    var(--fa-gloss),
    rgba(200,238,255,0.90) !important;
  border: 1px solid rgba(255,255,255,0.72) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

button.ytSpecButtonShapeNextTonal:not(.ytSpecButtonShapeNextSegmentedStart):not(.ytSpecButtonShapeNextSegmentedEnd):hover {
  background:
    var(--fa-gloss),
    rgba(165,225,252,0.98) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-2px) scale(1.04) !important;
}

button.ytSpecButtonShapeNextTonal:not(.ytSpecButtonShapeNextSegmentedStart):not(.ytSpecButtonShapeNextSegmentedEnd):active {
  transform: translateY(0px) scale(0.97) !important;
  box-shadow: var(--fa-shadow-active) !important;
}

/* Like / dislike / action buttons */
.yt-spec-button-shape-next--tonal:hover {
  box-shadow:
    0 0 12px rgba(18,168,221,0.22),
    0 0 24px rgba(18,168,221,0.10) !important;
}

/* Filled CTA buttons (Subscribe when not subscribed) */
.yt-spec-button-shape-next--mono.yt-spec-button-shape-next--filled {
  border-radius: var(--fa-radius-pill) !important;
  background:
    var(--fa-gloss),
    linear-gradient(to bottom, #42d0f8, #0e9ed5) !important;
  border: 1px solid rgba(255,255,255,0.55) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
  position: relative;
  overflow: hidden;
  cursor: pointer !important;
}

.yt-spec-button-shape-next--mono.yt-spec-button-shape-next--filled:hover {
  transform: translateY(-2px) scale(1.04) !important;
  background:
    var(--fa-gloss),
    linear-gradient(to bottom, #65deff, #14b8f0) !important;
  animation: aeroAquaPulse 4s ease-in-out infinite !important;
  color: white !important;
}

/* Subscribe button — aqua glossy gel */
ytd-subscribe-button-renderer button.ytSpecButtonShapeNextTonal {
  background:
    var(--fa-gloss),
    linear-gradient(to bottom, rgba(190,235,255,0.95) 0%, rgba(155,218,252,0.90) 100%) !important;
  border: 1px solid rgba(18,168,221,0.32) !important;
  border-radius: var(--fa-radius-pill) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
  position: relative;
  overflow: hidden;
  cursor: pointer !important;
}

ytd-subscribe-button-renderer button.ytSpecButtonShapeNextTonal:hover {
  transform: translateY(-2px) scale(1.06) !important;
  color: #0a2a4a !important;
  animation: aeroAquaPulse 4s ease-in-out infinite !important;
}

ytd-subscribe-button-renderer button {
  box-shadow:
    0 0 10px rgba(18,168,221,0.28),
    0 0 20px rgba(18,168,221,0.12) !important;
  transition: box-shadow var(--fa-bounce), transform var(--fa-bounce) !important;
}

ytd-subscribe-button-renderer button:hover {
  box-shadow:
    0 0 18px rgba(18,168,221,0.50),
    0 0 36px rgba(53,199,240,0.24) !important;
  transform: scale(1.05) !important;
}

/* Segmented buttons (Like / Dislike pair) */
.yt-spec-button-shape-next--call-to-action.yt-spec-button-shape-next--filled,
.yt-spec-button-shape-next--overlay.yt-spec-button-shape-next--filled {
  border: none;
  border-radius: var(--fa-radius-pill) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

.yt-spec-button-shape-next--call-to-action.yt-spec-button-shape-next--filled:hover,
.yt-spec-button-shape-next--overlay.yt-spec-button-shape-next--filled:hover {
  transform: translateY(-1px) scale(1.06) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

.yt-spec-button-shape-next--call-to-action.yt-spec-button-shape-next--text:hover {
  border: none !important;
  box-shadow: var(--fa-shadow-base) !important;
  background: rgba(170,228,255,0.52) !important;
  border-radius: var(--fa-radius-pill) !important;
  transition: box-shadow var(--fa-bounce) !important;
}

/* Mono text buttons */
.yt-spec-button-shape-next--mono.yt-spec-button-shape-next--text {
  margin-right: 0 !important;
  border-radius: var(--fa-radius-pill) !important;
  z-index: 30000 !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

.yt-spec-button-shape-next--mono.yt-spec-button-shape-next--text:hover {
  transform: translateY(-1px) scale(1.04) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  background: rgba(170,228,255,0.52) !important;
}

.yt-spec-button-shape-next--overlay.yt-spec-button-shape-next--text:hover {
  background: rgba(170,228,255,0.52) !important;
}

/* Guide button */
yt-icon-button#guide-button {
  border-radius: var(--fa-radius-pill) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

yt-icon-button#guide-button:hover {
  background:
    var(--fa-gloss),
    rgba(170,228,255,0.70) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-1px) scale(1.06);
}

yt-icon-button#guide-button:active {
  transform: scale(0.95) !important;
  box-shadow: var(--fa-shadow-active) !important;
}

/* Create / Upload button in masthead */
#buttons.ytd-masthead ytd-button-renderer yt-button-shape button {
  border-radius: var(--fa-radius-pill) !important;
  background:
    var(--fa-gloss),
    rgba(200,238,255,0.92) !important;
  border: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

#buttons.ytd-masthead ytd-button-renderer yt-button-shape button:hover {
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-2px) scale(1.06);
  background:
    var(--fa-gloss),
    rgba(165,225,252,0.98) !important;
}

#buttons.ytd-masthead ytd-button-renderer yt-button-shape button:active {
  transform: scale(0.97) !important;
  box-shadow: var(--fa-shadow-active) !important;
}

/* Notification bell */
ytd-notification-topbar-button-renderer.ytd-masthead yt-icon-button#icon {
  border-radius: var(--fa-radius-pill) !important;
  padding: 4px !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

ytd-notification-topbar-button-renderer.ytd-masthead yt-icon-button#icon:hover {
  background:
    var(--fa-gloss),
    rgba(170,228,255,0.70) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-1px) scale(1.1);
}

/* ============================================================
   FILTER CHIPS — Aqua glossy pill chips
   ============================================================ */
#description.ytd-watch-metadata,
ytd-browse[page-subtype="home"] #chips:not(ytd-guide-renderer #sections) {
  position: relative !important;
  overflow: hidden !important;
  padding: 0 10px !important;
  margin: 16px 16px 0 0 !important;
  border-radius: 3000px !important;
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.90) !important;
  border: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition: box-shadow var(--fa-bounce) !important;
}

ytd-guide-renderer #sections {
  margin-left: 8px !important;
}

ytd-browse[page-subtype="home"] #chips:not(ytd-guide-renderer #sections),
ytd-browse[page-subtype="home"] #chips {
  margin: 16px 0 0 0 !important;
  border-radius: 3000px !important;
  box-shadow: var(--fa-shadow-active) !important;
  position: relative !important;
  overflow: hidden !important;
  padding: 0 10px !important;
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.90) !important;
  border: 1px solid var(--fa-glass-border) !important;
  transition: box-shadow var(--fa-bounce) !important;
}

.yt-chip-shape {
  background-color: transparent !important;
}

.ytChipShapeChip {
  border-radius: var(--fa-radius-pill) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

.ytChipShapeChip:hover {
  transform: translateY(-1px) scale(1.06) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  background:
    var(--fa-gloss),
    rgba(155,218,252,0.80) !important;
}

/* Chip / filter pills — subtle edge glow on hover */
yt-chip-cloud-chip-renderer:hover {
  box-shadow:
    0 0 10px rgba(18,168,221,0.28),
    inset 0 0 8px rgba(255,255,255,0.72) !important;
}

iron-selector.yt-chip-cloud-renderer {
  padding-left: 5px !important;
}

ytd-search[has-search-header][has-bigger-thumbs] #header.ytd-search {
  margin: 5px auto !important;
}

/* ============================================================
   VIDEO CARDS — Frosted Aero glass tile
   ============================================================ */
#contents.ytd-page-manager {
  margin-top: 0;
  margin-left: 16px;
}

#contents.ytd-rich-grid-renderer {
  margin-left: 30px !important;
  margin-right: 20px !important;
}

/* Home feed video cards — aqua rim glow */
ytd-rich-grid-media {
  box-shadow:
    0 0 0 1px rgba(18,168,221,0.12),
    0px 4px 20px rgba(18,168,221,0.14),
    0px 1px 4px rgba(0,0,0,0.05) !important;
  transition: box-shadow var(--fa-bounce), transform var(--fa-bounce) !important;
}

ytd-rich-grid-media:hover {
  box-shadow:
    0 0 0 1.5px rgba(18,168,221,0.32),
    0px 8px 32px rgba(18,168,221,0.28),
    0px 0px 20px rgba(53,199,240,0.18) !important;
  transform: translateY(-3px) !important;
}

ytd-rich-grid-media #dismissible, ytd-compact-video-renderer, ytd-playlist-panel-video-renderer {
  animation: aeroBreathe 9s infinite ease-in-out;
}

/* Shorts capsule hover */
ytd-rich-grid-slim-media {
  z-index: 1 !important;
}

ytd-rich-grid-slim-media:hover {
  z-index: 2000 !important;
}

ytd-rich-grid-slim-media #dismissible,
ytd-reel-item-renderer #dismissible {
  transition: all var(--fa-bounce) !important;
  border-radius: 24px !important;
  border: 1px solid transparent !important;
}

ytd-rich-grid-slim-media:hover #dismissible,
ytd-reel-item-renderer:hover #dismissible {
  border-radius: 24px !important;
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.92) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  border: 1px solid var(--fa-glass-border) !important;
  transform: translateY(-3px) scale(1.02) !important;
  padding-bottom: 12px !important;
  margin-bottom: -12px !important;
}

/* Thumbnails */
ytd-thumbnail:hover a.yt-simple-endpoint {
  box-shadow: 0 0 0 3px rgba(18,168,221,0.32) !important;
  border-radius: 12px !important;
}

/* ============================================================
   VIDEO PLAYER — Rounded aero-style
   ============================================================ */
video.video-stream.html5-main-video,
.html5-video-player {
  border-radius: 18px !important;
}

#player-container.ytd-video-preview {
  background: none !important;
}

/* Progress bar — aqua rounded */
.ytPlayerProgressBarDragContainer {
  margin-left: 15px !important;
  margin-right: 15px !important;
  margin-bottom: 5px !important;
}

.ytProgressBarLineProgressBarLine,
.ytProgressBarLineProgressBarBackground,
.ytProgressBarLineProgressBarLoaded,
.ytProgressBarLineProgressBarHovered,
.ytProgressBarLineProgressBarPlayed,
.ytThumbnailOverlayProgressBarHostWatchedProgressBarSegment {
  border-radius: 20px !important;
}

/* Ambient mode */
body.yt-pro-ambient #cinematics.ytd-watch-flexy {
  filter: saturate(180%) blur(12px) contrast(1.05) brightness(1.2);
  transform: scale(3);
  opacity: 1;
  mix-blend-mode: lighten;
}

body.yt-pro-ambient {
  overflow-x: hidden !important;
}

body.yt-pro-ambient .style-scope.ytd-watch-flexy#player-container-inner {
  overflow: hidden;
  border-radius: 18px !important;
  box-shadow:
    0px 16px 40px rgba(18,168,221,0.22),
    0px 0px 8px rgba(53,199,240,0.16);
}

/* ============================================================
   PLAYER CONTROLS — Gel rounded controls
   ============================================================ */
.ytp-delhi-modern .ytp-chrome-controls .ytp-right-controls {
  background: none !important;
  border: none !important;
  border-radius: var(--fa-radius-lg) !important;
  overflow: hidden !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

.ytp-delhi-modern .ytp-chrome-controls .ytp-right-controls:hover {
  background:
    var(--fa-gloss),
    rgba(185,232,255,0.80) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-1px) scale(1.02);
}

.ytp-delhi-modern .ytp-chrome-controls .ytp-right-controls:active {
  transform: scale(0.98);
  box-shadow: var(--fa-shadow-active) !important;
}

.ytp-delhi-modern
  .ytp-chrome-controls
  .ytp-button:not(.ytp-right-controls .ytp-button, .ytp-volume-icon.ytp-button),
.ytp-delhi-modern .ytp-time-wrapper:not(.ytp-miniplayer-ui *),
.ytp-delhi-modern.ytp-delhi-horizontal-volume-controls
  .ytp-chrome-bottom.ytp-volume-slider-active
  .ytp-volume-area {
  background: none !important;
  border: none !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
  border-radius: var(--fa-radius-pill) !important;
}

.ytp-delhi-modern
  .ytp-chrome-controls
  .ytp-button:not(.ytp-right-controls .ytp-button, .ytp-volume-icon.ytp-button):hover,
.ytp-delhi-modern .ytp-time-wrapper:not(.ytp-miniplayer-ui *):hover,
.ytp-delhi-modern.ytp-delhi-horizontal-volume-controls
  .ytp-chrome-bottom.ytp-volume-slider-active
  .ytp-volume-area:hover {
  transform: translateY(-1px) scale(1.1) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  background:
    var(--fa-gloss),
    rgba(185,232,255,0.80) !important;
}

.ytp-delhi-modern
  .ytp-chrome-controls
  .ytp-button:not(.ytp-right-controls .ytp-button, .ytp-volume-icon.ytp-button):active,
.ytp-delhi-modern .ytp-time-wrapper:not(.ytp-miniplayer-ui *):active,
.ytp-delhi-modern.ytp-delhi-horizontal-volume-controls
  .ytp-chrome-bottom.ytp-volume-slider-active
  .ytp-volume-area:active {
  transform: scale(0.95);
  box-shadow: var(--fa-shadow-active) !important;
}

.ytp-delhi-modern.ytp-delhi-horizontal-volume-controls .ytp-volume-area {
  background: none !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

.ytp-delhi-modern.ytp-delhi-horizontal-volume-controls .ytp-volume-area:hover {
  background:
    var(--fa-gloss),
    rgba(185,232,255,0.80) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transform: translateY(-1px) scale(1.1);
}

.ytp-fullscreen-grid-expand-button {
  background: none !important;
  border: 1px solid rgba(18,168,221,0.22) !important;
  border-radius: var(--fa-radius-sm) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

.ytp-fullscreen-grid-expand-button:hover {
  background:
    var(--fa-gloss),
    rgba(185,232,255,0.80) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

.ytPlayerQuickActionButtonsHost:not(:empty) {
  background-color: rgba(200,238,255,0.88) !important;
}

.ytInlinePlayerControlsTopRightControlsCircleButton {
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.90) !important;
  border: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
  cursor: pointer !important;
}

.ytInlinePlayerControlsTopRightControlsCircleButton:hover {
  transform: translateY(-1px) scale(1.1) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

/* Player settings panel */
.ytp-panel,
.ytp-popup.ytp-settings-menu {
  background:
    var(--fa-gloss),
    rgba(220,244,255,0.97) !important;
}

.ytp-panel {
  border: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  transition: box-shadow var(--fa-bounce) !important;
  border-radius: 20px !important;
  padding: 0 5px !important;
}

.ytp-menuitem:not([aria-disabled="true"]) {
  margin-left: 100px !important;
  border-radius: var(--fa-radius-sm) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

.ytp-menuitem:not([aria-disabled="true"]):hover {
  border-radius: var(--fa-radius-sm) !important;
  box-shadow: var(--fa-shadow-base) !important;
  background-color: rgba(155,218,252,0.50) !important;
}

.ytp-delhi-modern .ytp-popup {
  background: none !important;
  border-radius: 20px !important;
}

.ytp-popup.ytp-delhi-modern-contextmenu {
  background: none !important;
  padding: 0 5px !important;
  border-radius: 20px !important;
  box-shadow: none !important;
}

.ytp-delhi-modern .ytp-chapter-container {
  width: 100% !important;
  padding-left: 10px;
}

/* ============================================================
   MENUS & DROPDOWNS — Frosted sky-blue containers
   ============================================================ */
ytd-menu-popup-renderer,
ytd-video-preview,
#contentWrapper.tp-yt-iron-dropdown > *,
tp-yt-paper-dialog {
  background:
    var(--fa-gloss),
    rgba(220,244,255,0.97) !important;
  border-radius: 24px !important;
  border: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  backdrop-filter: blur(12px) !important;
  -webkit-backdrop-filter: blur(12px) !important;
  transform: scale(1)
    translate(
      var(--ytd-video-preview-translate-left),
      var(--ytd-video-preview-translate-top)
    );
  transition:
    opacity 0.2s ease-in,
    transform 0.16s ease-out 0.2s;
  max-height: 80vh !important;
  overflow-y: auto !important;
  max-width: none !important;
}

tp-yt-paper-dialog {
  border-radius: 24px !important;
}

ytd-simple-menu-header-renderer {
  background: none !important;
}

#items.yt-multi-page-menu-section-renderer
  > *.yt-multi-page-menu-section-renderer:not(
    [compact-link-style="compact-link-style-type-disclaimer"]
  ):not(ytd-message-renderer) {
  margin: 10px;
  border: 1px solid rgba(18,168,221,0.10) !important;
  border-radius: var(--fa-radius-md) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

#items.yt-multi-page-menu-section-renderer
  > *.yt-multi-page-menu-section-renderer:not(
    [compact-link-style="compact-link-style-type-disclaimer"]
  ):not(ytd-message-renderer):hover {
  transform: translateY(-1px) scale(1.02);
  box-shadow: var(--fa-shadow-hover) !important;
  background: rgba(155,218,252,0.50) !important;
}

ytd-menu-service-item-renderer,
.yt-list-item-view-model__container--tappable,
tp-yt-paper-item.ytd-menu-service-item-download-renderer {
  border-radius: var(--fa-radius-md) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

ytd-menu-service-item-renderer:hover,
.yt-list-item-view-model__container--tappable:hover,
tp-yt-paper-item.ytd-menu-service-item-download-renderer:hover {
  transform: translateY(-1px) scale(1.02) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  background: rgba(155,218,252,0.50) !important;
}

ytd-menu-service-item-renderer[use-list-item-styles]
  tp-yt-paper-item.ytd-menu-service-item-renderer,
.ytContextualSheetLayoutHost {
  background: none !important;
  padding: 13px !important;
}

tp-yt-paper-listbox {
  padding: 10px !important;
}

tp-yt-paper-listbox.yt-dropdown-menu {
  background:
    var(--fa-gloss),
    rgba(220,244,255,0.97) !important;
  border-radius: 16px !important;
}

tp-yt-paper-listbox.yt-dropdown-menu tp-yt-paper-item.yt-dropdown-menu:hover {
  background: rgba(155,218,252,0.50) !important;
}

tp-yt-paper-listbox.yt-dropdown-menu .iron-selected.yt-dropdown-menu {
  background: rgba(130,205,248,0.55) !important;
  border-radius: var(--fa-radius-md) !important;
}

a.yt-simple-endpoint.yt-dropdown-menu {
  border-radius: var(--fa-radius-md) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

a.yt-simple-endpoint.yt-dropdown-menu:hover {
  transform: translateY(-1px) scale(1.04) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  background: rgba(155,218,252,0.50) !important;
}

#trigger.tp-yt-paper-menu-button {
  padding: 10px !important;
  border-radius: var(--fa-radius-md) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

#trigger.tp-yt-paper-menu-button:hover {
  transform: translateY(-1px) scale(1.1) !important;
  background:
    var(--fa-gloss),
    rgba(155,218,252,0.70) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

/* ============================================================
   TOOLTIPS — Rounded aqua bubble
   ============================================================ */
.tp-yt-paper-tooltip[style-target="tooltip"] {
  border: 1px solid var(--fa-glass-border) !important;
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.97) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  color: #0a2a4a !important;
  border-radius: var(--fa-radius-pill) !important;
}

html
  body[rounded-container]
  tp-yt-paper-tooltip
  .tp-yt-paper-tooltip[style-target="tooltip"] {
  border-radius: 20px !important;
  padding: 7px 14px;
}

/* ============================================================
   LOGO
   ============================================================ */
ytd-topbar-logo-renderer {
  border-radius: var(--fa-radius-md) !important;
  padding: 0px !important;
  max-height: 45px !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

ytd-topbar-logo-renderer:hover {
  transform: translateY(-1px) scale(1.04) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

/* ============================================================
   WATCH PAGE PANELS — Acrylic sidebar & description
   ============================================================ */
#description.ytd-watch-metadata {
  border-radius: 24px !important;
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.88) !important;
  border: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-base) !important;
}

ytd-expandable-metadata-renderer:not([is-expanded]) {
  border-radius: var(--fa-radius-md) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

ytd-expandable-metadata-renderer:not([is-expanded]):hover {
  box-shadow: var(--fa-shadow-hover) !important;
}

ytd-expandable-metadata-renderer {
  padding: 5px !important;
  border-radius: 24px !important;
}

ytd-expandable-metadata-renderer:not([is-expanded])
  #header.ytd-expandable-metadata-renderer:hover {
  background: none !important;
}

/* ============================================================
   WATCH PAGE SIDEBAR — Floating aqua glass cards
   ============================================================ */
ytd-watch-next-secondary-results-renderer,
ytd-watch-next-secondary-results-renderer #items,
#related.ytd-watch-flexy {
  background: transparent !important;
  border: none !important;
  border-radius: 0 !important;
  overflow: visible !important;
}

ytd-compact-video-renderer,
ytd-compact-playlist-renderer,
ytd-compact-radio-renderer {
  background:
    var(--fa-gloss),
    rgba(215,242,255,0.90) !important;
  border: 1px solid var(--fa-glass-border) !important;
  border-radius: 12px !important;
  box-shadow:
    0px 4px 16px rgba(18,168,221,0.16),
    0px 0px 8px rgba(18,168,221,0.08),
    inset 0 1px 3px rgba(255,255,255,0.90) !important;
  padding: 8px !important;
  margin-bottom: 10px !important;
  overflow: visible !important;
  transition: transform var(--fa-bounce), box-shadow var(--fa-bounce) !important;
}

ytd-compact-video-renderer:hover,
ytd-compact-playlist-renderer:hover,
ytd-compact-radio-renderer:hover {
  transform: translateY(-2px) scale(1.01) !important;
  box-shadow:
    0px 8px 28px rgba(18,168,221,0.30),
    0px 0px 16px rgba(53,199,240,0.20),
    inset 0 1px 5px rgba(255,255,255,0.95) !important;
  background:
    var(--fa-gloss),
    rgba(195,234,255,0.95) !important;
}

ytd-compact-video-renderer:focus-within {
  box-shadow:
    0px 0px 0px 2px rgba(18,168,221,0.42),
    0px 8px 28px rgba(18,168,221,0.30) !important;
}

ytd-compact-video-renderer ytd-thumbnail {
  border-radius: 8px !important;
  overflow: hidden !important;
  margin-right: 12px !important;
}

/* ============================================================
   PLAYLIST PANEL
   ============================================================ */
.anchored-panel.ytd-shorts {
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.92) !important;
  padding: 5px;
  border: 1px solid var(--fa-glass-border) !important;
  border-radius: 24px !important;
  box-shadow: var(--fa-shadow-hover) !important;
  backdrop-filter: blur(12px) !important;
  -webkit-backdrop-filter: blur(12px) !important;
  margin-top: 15px !important;
}

ytd-engagement-panel-title-header-renderer[shorts-panel][enable-anchored-panel]
  #header.ytd-engagement-panel-title-header-renderer {
  border-bottom: none;
}

ytd-engagement-panel-section-list-renderer[match-content-theme]
  #content.ytd-engagement-panel-section-list-renderer {
  background: none !important;
}

ytd-engagement-panel-section-list-renderer:not([live-chat-engagement-panel]) {
  border: 1px solid rgba(18,168,221,0.10) !important;
  border-radius: 24px !important;
}

#subheader.ytd-engagement-panel-title-header-renderer:not(:empty) {
  border-radius: var(--fa-radius-xl) !important;
  border: 1px solid rgba(18,168,221,0.16) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

ytd-comments-header-renderer[engagement-panel] {
  border-radius: 20px !important;
  margin: 10px !important;
  border: 1px solid var(--fa-glass-border) !important;
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.85) !important;
  box-shadow: var(--fa-shadow-base) !important;
}

ytd-item-section-renderer[static-comments-header][enable-anchored-panel]
  #header.ytd-item-section-renderer {
  background: none !important;
}

.header.ytd-playlist-panel-renderer {
  background: none !important;
}

#container.ytd-playlist-panel-renderer {
  border: 1px solid var(--fa-glass-border) !important;
  border-radius: 20px !important;
}

ytd-playlist-panel-video-renderer[selected][use-color-palette],
ytd-playlist-panel-video-renderer[selected][use-color-palette]:hover:not(.dragging) {
  border-radius: 20px !important;
  padding: 10px !important;
  background: rgba(145,212,248,0.50) !important;
  box-shadow: var(--fa-shadow-active) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

ytd-playlist-panel-video-renderer.ytd-playlist-panel-renderer {
  border-radius: 20px !important;
  padding: 10px !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
  margin: 5px 0 !important;
}

ytd-playlist-panel-video-renderer.ytd-playlist-panel-renderer:hover {
  box-shadow: var(--fa-shadow-hover) !important;
  background: rgba(155,218,252,0.38) !important;
}

ytd-playlist-video-renderer:not(.dragging) {
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
  margin: 5px;
  border-radius: 16px !important;
}

ytd-playlist-video-renderer:not(.dragging):hover {
  box-shadow: var(--fa-shadow-hover) !important;
  background: rgba(155,218,252,0.32) !important;
}

.immersive-header-container.ytd-playlist-header-renderer {
  border: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-base) !important;
  border-radius: 24px !important;
}

.ytContextualSheetLayoutContentContainer,
.ytChipBarViewModelHost,
.immersive-header-content.ytd-playlist-header-renderer {
  overflow: visible !important;
}

/* ============================================================
   THUMBNAIL OVERLAYS & BADGES
   ============================================================ */
ytd-thumbnail-overlay-toggle-button-renderer {
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.90) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
  border-radius: var(--fa-radius-lg) !important;
  border: 1px solid rgba(255,255,255,0.65) !important;
}

ytd-thumbnail-overlay-toggle-button-renderer:hover {
  transform: translateY(-1px) scale(1.1) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

ytd-thumbnail-overlay-toggle-button-renderer[use-expandable-tooltip][hovered]
  #label.ytd-thumbnail-overlay-toggle-button-renderer {
  background-color: rgba(210,242,255,0.95) !important;
  margin-right: 10px;
  border-radius: 12px !important;
  box-shadow: var(--fa-shadow-hover) !important;
  color: #0a2a4a !important;
}

.yt-badge-shape--thumbnail-default {
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.90) !important;
  border-radius: var(--fa-radius-md) !important;
  border: 1px solid rgba(255,255,255,0.68) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

.yt-spec-touch-feedback-shape--thumbnail-size-large
  .yt-spec-touch-feedback-shape__stroke,
.yt-spec-touch-feedback-shape--thumbnail-size-large
  .yt-spec-touch-feedback-shape__fill,
.yt-spec-touch-feedback-shape--thumbnail-size-large
  .yt-spec-touch-feedback-shape__hover-effect {
  padding: 100px !important;
  border-radius: 24px !important;
}

/* ============================================================
   CHAPTER MARKERS
   ============================================================ */
ytd-macro-markers-list-item-renderer[modern][layout="MACRO_MARKERS_LIST_ITEM_RENDERER_LAYOUT_VERTICAL"]:not(
    [carousel-type="MACRO_MARKERS_LIST_ITEM_RENDERER_CAROUSEL_TYPE_TEXT_ONLY"]
  ) {
  border-radius: 16px !important;
  border: 1px solid rgba(18,168,221,0.12) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

ytd-macro-markers-list-item-renderer[modern][layout="MACRO_MARKERS_LIST_ITEM_RENDERER_LAYOUT_VERTICAL"]:not(
    [carousel-type="MACRO_MARKERS_LIST_ITEM_RENDERER_CAROUSEL_TYPE_TEXT_ONLY"]
  ):hover {
  box-shadow: var(--fa-shadow-hover) !important;
  background: rgba(155,218,252,0.32) !important;
}

ytd-macro-markers-list-item-renderer {
  border-radius: 12px !important;
  margin: 5px;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

ytd-macro-markers-list-item-renderer:hover {
  transform: translateY(-1px) scale(1.02) !important;
  box-shadow: var(--fa-shadow-hover) !important;
}

/* ============================================================
   ALERTS & INFO BANNERS
   ============================================================ */
ytd-alert-with-button-renderer[type="INFO"] {
  box-shadow: var(--fa-shadow-base) !important;
  border-radius: 20px !important;
  border: 1px solid rgba(18,168,221,0.16) !important;
  background:
    var(--fa-gloss),
    rgba(210,242,255,0.88) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce) !important;
}

/* ============================================================
   LIKE BUTTON HOST
   ============================================================ */
.ytLikeButtonViewModelHost {
  margin-right: 5px;
}

.yt-spec-button-shape-next--size-m.yt-spec-button-shape-next--segmented-start:hover::after {
  background: none !important;
}

/* ============================================================
   SEARCH PAGE RESULTS
   ============================================================ */
ytd-video-renderer, ytd-channel-renderer, ytd-playlist-renderer {
  background: var(--fa-gloss), rgba(215,242,255,0.85) !important;
  border: 1px solid var(--fa-glass-border) !important;
  border-radius: var(--fa-radius-lg) !important;
  padding: 16px !important;
  margin-bottom: 24px !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition: transform var(--fa-bounce), box-shadow var(--fa-bounce) !important;
}

ytd-video-renderer:hover, ytd-channel-renderer:hover, ytd-playlist-renderer:hover {
  transform: translateY(-3px) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  background: var(--fa-gloss), rgba(195,234,255,0.95) !important;
}

/* ============================================================
   CHANNEL PAGE
   ============================================================ */
#channel-header.ytd-c4-tabbed-header-renderer {
  background: var(--fa-gloss), rgba(210,240,255,0.85) !important;
  border-bottom: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-base) !important;
  backdrop-filter: blur(12px) !important;
}

#tabs-inner-container.ytd-c4-tabbed-header-renderer {
  background: rgba(185,228,255,0.70) !important;
  border-radius: var(--fa-radius-md) !important;
  margin: 10px;
  padding: 5px;
}

tp-yt-paper-tab.ytd-c4-tabbed-header-renderer {
  border-radius: var(--fa-radius-sm) !important;
  transition: background var(--fa-bounce), transform var(--fa-bounce);
}

tp-yt-paper-tab.ytd-c4-tabbed-header-renderer.iron-selected {
  background: rgba(255,255,255,0.82) !important;
  box-shadow: 0 2px 8px rgba(18,168,221,0.22) !important;
}

/* ============================================================
   DIALOGS & POPUPS
   ============================================================ */
tp-yt-paper-dialog, ytd-hotkey-dialog-renderer {
  background: var(--fa-gloss), rgba(220,244,255,0.95) !important;
  border-radius: var(--fa-radius-xl) !important;
  border: 2px solid var(--fa-glass-border) !important;
  box-shadow:
    0px 12px 48px rgba(18,168,221,0.28),
    inset 0 2px 10px rgba(255,255,255,0.90) !important;
  backdrop-filter: blur(18px) !important;
}

/* ============================================================
   SETTINGS PAGE
   ============================================================ */
#header.ytd-settings-sidebar-renderer {
  background: transparent !important;
}

ytd-settings-sidebar-renderer {
  background: var(--fa-gloss), rgba(210,240,255,0.80) !important;
  border-radius: var(--fa-radius-lg) !important;
  border: 1px solid var(--fa-glass-border) !important;
  box-shadow: var(--fa-shadow-base) !important;
  padding: 16px !important;
}

/* ============================================================
   FORMS, TOGGLES, SLIDERS
   ============================================================ */
tp-yt-paper-toggle-button[checked] .toggle-button.tp-yt-paper-toggle-button {
  background-color: var(--fa-aqua-500) !important;
  box-shadow: 0 0 10px var(--fa-aqua-500), inset 0 2px 4px rgba(255,255,255,0.80) !important;
}

tp-yt-paper-toggle-button[checked] .toggle-bar.tp-yt-paper-toggle-button {
  background-color: var(--fa-aqua-400) !important;
  opacity: 0.6 !important;
}

/* ============================================================
   COMMENTS & CHAT
   ============================================================ */
ytd-comment-simplebox-renderer, yt-live-chat-message-input-renderer {
  background: rgba(255,255,255,0.60) !important;
  border-radius: var(--fa-radius-md) !important;
  border: 1px solid var(--fa-glass-border) !important;
  padding: 12px !important;
  box-shadow: inset 0 2px 6px rgba(18,168,221,0.10) !important;
}

/* ============================================================
   SCROLLBAR — Aqua scrollbar
   ============================================================ */
::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

::-webkit-scrollbar-track {
  background: rgba(191,230,255,0.30);
  border-radius: 10px;
}

::-webkit-scrollbar-thumb {
  background: linear-gradient(to bottom, rgba(53,199,240,0.45), rgba(11,134,194,0.38));
  border-radius: 10px;
  border: 1px solid rgba(255,255,255,0.55);
}

::-webkit-scrollbar-thumb:hover {
  background: linear-gradient(to bottom, rgba(53,199,240,0.70), rgba(11,134,194,0.58));
}

/* ============================================================
   FORCE LIGHT TEXT FOR DARK ELEMENTS ON LIGHT BACKGROUND
   ============================================================ */
html[data-ypp-ui-style="frutiger-aero"] yt-formatted-string,
html[data-ypp-ui-style="frutiger-aero"] span,
html[data-ypp-ui-style="frutiger-aero"] a,
html[data-ypp-ui-style="frutiger-aero"] h1,
html[data-ypp-ui-style="frutiger-aero"] h2,
html[data-ypp-ui-style="frutiger-aero"] h3,
html[data-ypp-ui-style="frutiger-aero"] p,
html[data-ypp-ui-style="frutiger-aero"] #video-title,
html[data-ypp-ui-style="frutiger-aero"] .ytd-video-meta-block,
html[data-ypp-ui-style="frutiger-aero"] #metadata-line span,
html[data-ypp-ui-style="frutiger-aero"] yt-formatted-string.ytd-channel-name,
html[data-ypp-ui-style="frutiger-aero"] #channel-name a,
html[data-ypp-ui-style="frutiger-aero"] .ytd-channel-name,
html[data-ypp-ui-style="frutiger-aero"] ytd-badge-supported-renderer span,
html[data-ypp-ui-style="frutiger-aero"] #title-wrapper yt-formatted-string,
html[data-ypp-ui-style="frutiger-aero"] .title-text,
html[data-ypp-ui-style="frutiger-aero"] ytd-rich-grid-renderer #title,
html[data-ypp-ui-style="frutiger-aero"] .shortsLockupViewModelHostThumbnailOverlayFooter,
html[data-ypp-ui-style="frutiger-aero"] ytd-guide-entry-renderer yt-formatted-string {
  color: #0a2a4a !important;
  text-shadow: none !important;
}

html[data-ypp-ui-style="frutiger-aero"] #video-title,
html[data-ypp-ui-style="frutiger-aero"] ytd-video-renderer #video-title,
html[data-ypp-ui-style="frutiger-aero"] ytd-rich-item-renderer #video-title {
  color: #0a2a4a !important;
}

html[data-ypp-ui-style="frutiger-aero"] #metadata-line span,
html[data-ypp-ui-style="frutiger-aero"] .ytd-video-meta-block span,
html[data-ypp-ui-style="frutiger-aero"] yt-formatted-string.ytd-channel-name {
  color: #1a4a78 !important;
}

/* Force light mode CSS variables */
html[data-ypp-ui-style="frutiger-aero"] {
  --yt-spec-text-primary: #0a2a4a !important;
  --yt-spec-text-secondary: #1a4a78 !important;
  --yt-spec-text-disabled: #5a8aaa !important;
  --yt-spec-text-primary-inverse: #ffffff !important;

  --yt-spec-icon-active-other: #0a2a4a !important;
  --yt-spec-icon-inactive: #1a4a78 !important;
  --yt-spec-icon-disabled: #5a8aaa !important;

  --yt-spec-base-background: transparent !important;
  --yt-spec-raised-background: rgba(210,242,255,0.95) !important;
  --yt-spec-menu-background: rgba(210,242,255,0.95) !important;

  color: var(--yt-spec-text-primary) !important;
}

/* ============================================================
   AQUA GLOW EFFECTS
   ============================================================ */

/* Home feed video cards — aqua border glow */
ytd-rich-grid-media {
  box-shadow:
    0 0 0 1px rgba(18,168,221,0.12),
    0px 4px 20px rgba(18,168,221,0.14),
    0px 1px 4px rgba(0,0,0,0.05) !important;
  transition: box-shadow var(--fa-bounce), transform var(--fa-bounce) !important;
}

ytd-rich-grid-media:hover {
  box-shadow:
    0 0 0 1.5px rgba(18,168,221,0.32),
    0px 8px 32px rgba(18,168,221,0.28),
    0px 0px 20px rgba(53,199,240,0.18) !important;
  transform: translateY(-3px) !important;
}

/* ============================================================
   PLAYLIST REDESIGN — Frutiger Aero Sky Glass
   ============================================================ */

/* Playlist Sidebar */
#ypp-pl-root .ypp-pl-sidebar {
  background:
    linear-gradient(
      160deg,
      rgba(200,238,255,0.88) 0%,
      rgba(185,228,252,0.82) 100%
    ) !important;
  backdrop-filter: blur(40px) saturate(1.4) !important;
  -webkit-backdrop-filter: blur(40px) saturate(1.4) !important;
  border: 1px solid rgba(18,168,221,0.28) !important;
  border-top: 1px solid rgba(255,255,255,0.72) !important;
  box-shadow:
    var(--fa-shadow-base),
    inset 0 1px 0 rgba(255,255,255,0.82),
    0 0 40px rgba(18,168,221,0.10) !important;
  border-radius: var(--fa-radius-xl) !important;
}

#ypp-pl-root .ypp-pl-sidebar::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: var(--fa-radius-xl);
  background: var(--fa-gloss);
  pointer-events: none;
  z-index: 0;
}

/* Playlist Toolbar */
#ypp-pl-root .ypp-pl-toolbar {
  background:
    linear-gradient(
      to right,
      rgba(185,228,255,0.70) 0%,
      rgba(170,220,252,0.65) 100%
    ) !important;
  backdrop-filter: blur(20px) !important;
  border-bottom: 1px solid rgba(18,168,221,0.18) !important;
  border-radius: var(--fa-radius-md) var(--fa-radius-md) 0 0 !important;
}

/* Playlist Video Cards */
#ypp-pl-root .ypp-pl-card {
  background:
    linear-gradient(
      135deg,
      rgba(215,242,255,0.75) 0%,
      rgba(200,234,255,0.68) 100%
    ) !important;
  border: 1px solid rgba(18,168,221,0.20) !important;
  border-radius: var(--fa-radius-lg) !important;
  box-shadow: var(--fa-shadow-base) !important;
  transition:
    transform var(--fa-bounce),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

#ypp-pl-root .ypp-pl-card:hover {
  background:
    linear-gradient(
      135deg,
      rgba(195,230,255,0.88) 0%,
      rgba(175,220,252,0.82) 100%
    ) !important;
  box-shadow: var(--fa-shadow-hover) !important;
  border-color: rgba(18,168,221,0.40) !important;
  transform: translateY(-2px) scale(1.01) !important;
}

/* 1-Column List View */
#ypp-pl-root .ypp-pl-cols-1 .ypp-pl-card {
  background: rgba(195,232,255,0.35) !important;
  border: 1px solid rgba(18,168,221,0.15) !important;
  box-shadow: none !important;
  border-radius: var(--fa-radius-md) !important;
}

#ypp-pl-root .ypp-pl-cols-1 .ypp-pl-card:hover {
  background: rgba(175,222,252,0.55) !important;
  transform: none !important;
  border-color: rgba(18,168,221,0.28) !important;
}

/* Playlist Buttons — glossy aqua capsules */
#ypp-pl-root .ypp-pl-btn-play,
#ypp-pl-root .ypp-pl-btn-shuffle,
#ypp-pl-root .ypp-pl-btn-tool,
#ypp-pl-root .ypp-pl-filter,
#ypp-pl-root .ypp-pl-col-switcher {
  background:
    linear-gradient(
      to bottom,
      rgba(255,255,255,0.88) 0%,
      rgba(185,228,252,0.82) 45%,
      rgba(145,208,248,0.88) 100%
    ) !important;
  border: 1px solid rgba(18,168,221,0.42) !important;
  border-radius: var(--fa-radius-pill) !important;
  box-shadow:
    var(--fa-shadow-base),
    inset 0 1px 0 rgba(255,255,255,0.92) !important;
  color: #0b3d6b !important;
  transition:
    transform var(--fa-bounce-alt),
    box-shadow var(--fa-bounce),
    background var(--fa-bounce) !important;
}

#ypp-pl-root .ypp-pl-btn-play:hover,
#ypp-pl-root .ypp-pl-btn-shuffle:hover,
#ypp-pl-root .ypp-pl-btn-tool:hover {
  background:
    linear-gradient(
      to bottom,
      rgba(255,255,255,0.96) 0%,
      rgba(160,218,252,0.92) 45%,
      rgba(120,198,248,0.94) 100%
    ) !important;
  box-shadow:
    var(--fa-shadow-hover),
    0 0 16px rgba(18,168,221,0.42),
    inset 0 1px 0 rgba(255,255,255,1.0) !important;
  transform: scale(1.05) translateY(-1px) !important;
}

/* Play button — solid aqua */
#ypp-pl-root .ypp-pl-btn-play {
  background:
    linear-gradient(
      135deg,
      rgba(53,199,240,0.90) 0%,
      rgba(11,134,194,0.85) 100%
    ) !important;
  color: #fff !important;
  border: none !important;
  box-shadow:
    0 4px 16px rgba(18,168,221,0.38),
    inset 0 1px 0 rgba(255,255,255,0.52) !important;
}

#ypp-pl-root .ypp-pl-btn-play:hover {
  background:
    linear-gradient(
      135deg,
      rgba(80,215,255,0.95) 0%,
      rgba(15,155,218,0.92) 100%
    ) !important;
  box-shadow:
    0 6px 24px rgba(18,168,221,0.58),
    0 0 20px rgba(53,199,240,0.32) !important;
}

/* Duration Info Card */
#ypp-pl-root .ypp-pl-duration-card {
  background:
    linear-gradient(
      135deg,
      rgba(200,236,255,0.55) 0%,
      rgba(185,228,255,0.50) 100%
    ) !important;
  border: 1px solid rgba(18,168,221,0.22) !important;
  border-radius: var(--fa-radius-lg) !important;
  box-shadow:
    inset 0 2px 12px rgba(18,168,221,0.10),
    var(--fa-shadow-base) !important;
}

#ypp-pl-root .ypp-pl-duration-grid {
  background: rgba(18,168,221,0.08) !important;
  border: 1px solid rgba(18,168,221,0.18) !important;
  border-radius: var(--fa-radius-md) !important;
}

/* Cover image */
#ypp-pl-root .ypp-pl-cover-wrap {
  border-radius: var(--fa-radius-lg) !important;
  box-shadow:
    var(--fa-shadow-base),
    0 0 20px rgba(18,168,221,0.16) !important;
}

/* Title / Owner text */
#ypp-pl-root .ypp-pl-title {
  color: #0a2a4a !important;
}

#ypp-pl-root .ypp-pl-owner,
#ypp-pl-root .ypp-pl-owner:hover {
  color: #0b5fa3 !important;
}

#ypp-pl-root .ypp-pl-stats {
  color: rgba(11,95,163,0.65) !important;
}

/* ==========================================================================
   BOUNCING WATER BUBBLES
   ========================================================================== */
.frutiger-bubbles-container {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: -1;
  overflow: hidden;
}

.fa-bubble {
  position: absolute;
  bottom: -150px;
  background: radial-gradient(circle at 30% 30%, rgba(255, 255, 255, 0.9) 0%, rgba(255, 255, 255, 0.1) 15%, transparent 60%, rgba(255, 255, 255, 0.6) 100%);
  border-radius: 50%;
  box-shadow: inset 0 0 10px rgba(255, 255, 255, 0.5), inset 10px 0 20px rgba(255, 255, 255, 0.2), inset -10px 0 20px rgba(53, 199, 240, 0.5);
  backdrop-filter: blur(2px);
}


.fa-bubble:nth-child(1) {
  width: 66px;
  height: 66px;
  left: 58vw;
  animation: faMove1 10s infinite linear;
  animation-delay: -0.9s;
}
@keyframes faMove1 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 17vh; transform: translateX(139px); opacity: 0; }
}

.fa-bubble:nth-child(2) {
  width: 86px;
  height: 86px;
  left: 2vw;
  animation: faMove2 6s infinite linear;
  animation-delay: -5.6s;
}
@keyframes faMove2 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 6vh; transform: translateX(-1px); opacity: 0; }
}

.fa-bubble:nth-child(3) {
  width: 9px;
  height: 9px;
  left: 65vw;
  animation: faMove3 14s infinite linear;
  animation-delay: -3.2s;
}
@keyframes faMove3 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 1vh; transform: translateX(-61px); opacity: 0; }
}

.fa-bubble:nth-child(4) {
  width: 80px;
  height: 80px;
  left: 59vw;
  animation: faMove4 4s infinite linear;
  animation-delay: -3.8s;
}
@keyframes faMove4 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 97vh; transform: translateX(-78px); opacity: 0; }
}

.fa-bubble:nth-child(5) {
  width: 80px;
  height: 80px;
  left: 7vw;
  animation: faMove5 4s infinite linear;
  animation-delay: -0.1s;
}
@keyframes faMove5 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 61vh; transform: translateX(-78px); opacity: 0; }
}

.fa-bubble:nth-child(6) {
  width: 48px;
  height: 48px;
  left: 26vw;
  animation: faMove6 12s infinite linear;
  animation-delay: -7.0s;
}
@keyframes faMove6 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 52vh; transform: translateX(115px); opacity: 0; }
}

.fa-bubble:nth-child(7) {
  width: 35px;
  height: 35px;
  left: 5vw;
  animation: faMove7 6s infinite linear;
  animation-delay: -5.3s;
}
@keyframes faMove7 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 14vh; transform: translateX(36px); opacity: 0; }
}

.fa-bubble:nth-child(8) {
  width: 94px;
  height: 94px;
  left: 84vw;
  animation: faMove8 6s infinite linear;
  animation-delay: -1.3s;
}
@keyframes faMove8 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 6vh; transform: translateX(-4px); opacity: 0; }
}

.fa-bubble:nth-child(9) {
  width: 49px;
  height: 49px;
  left: 53vw;
  animation: faMove9 5s infinite linear;
  animation-delay: -3.7s;
}
@keyframes faMove9 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 88vh; transform: translateX(-84px); opacity: 0; }
}

.fa-bubble:nth-child(10) {
  width: 88px;
  height: 88px;
  left: 74vw;
  animation: faMove10 6s infinite linear;
  animation-delay: -5.8s;
}
@keyframes faMove10 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 12vh; transform: translateX(118px); opacity: 0; }
}

.fa-bubble:nth-child(11) {
  width: 42px;
  height: 42px;
  left: 27vw;
  animation: faMove11 8s infinite linear;
  animation-delay: -5.9s;
}
@keyframes faMove11 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 12vh; transform: translateX(106px); opacity: 0; }
}

.fa-bubble:nth-child(12) {
  width: 44px;
  height: 44px;
  left: 5vw;
  animation: faMove12 4s infinite linear;
  animation-delay: -3.0s;
}
@keyframes faMove12 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 57vh; transform: translateX(53px); opacity: 0; }
}

.fa-bubble:nth-child(13) {
  width: 26px;
  height: 26px;
  left: 37vw;
  animation: faMove13 10s infinite linear;
  animation-delay: -5.4s;
}
@keyframes faMove13 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 51vh; transform: translateX(78px); opacity: 0; }
}

.fa-bubble:nth-child(14) {
  width: 35px;
  height: 35px;
  left: 26vw;
  animation: faMove14 14s infinite linear;
  animation-delay: -3.9s;
}
@keyframes faMove14 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 49vh; transform: translateX(118px); opacity: 0; }
}

.fa-bubble:nth-child(15) {
  width: 77px;
  height: 77px;
  left: 59vw;
  animation: faMove15 11s infinite linear;
  animation-delay: -4.0s;
}
@keyframes faMove15 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 16vh; transform: translateX(-52px); opacity: 0; }
}

.fa-bubble:nth-child(16) {
  width: 23px;
  height: 23px;
  left: 58vw;
  animation: faMove16 12s infinite linear;
  animation-delay: -0.5s;
}
@keyframes faMove16 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 64vh; transform: translateX(160px); opacity: 0; }
}

.fa-bubble:nth-child(17) {
  width: 67px;
  height: 67px;
  left: 38vw;
  animation: faMove17 3s infinite linear;
  animation-delay: -0.7s;
}
@keyframes faMove17 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 28vh; transform: translateX(34px); opacity: 0; }
}

.fa-bubble:nth-child(18) {
  width: 61px;
  height: 61px;
  left: 24vw;
  animation: faMove18 8s infinite linear;
  animation-delay: -2.3s;
}
@keyframes faMove18 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 69vh; transform: translateX(-49px); opacity: 0; }
}

.fa-bubble:nth-child(19) {
  width: 47px;
  height: 47px;
  left: 25vw;
  animation: faMove19 6s infinite linear;
  animation-delay: -0.4s;
}
@keyframes faMove19 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 21vh; transform: translateX(140px); opacity: 0; }
}

.fa-bubble:nth-child(20) {
  width: 83px;
  height: 83px;
  left: 78vw;
  animation: faMove20 13s infinite linear;
  animation-delay: -10.8s;
}
@keyframes faMove20 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 13vh; transform: translateX(-77px); opacity: 0; }
}

.fa-bubble:nth-child(21) {
  width: 77px;
  height: 77px;
  left: 22vw;
  animation: faMove21 14s infinite linear;
  animation-delay: -13.0s;
}
@keyframes faMove21 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 29vh; transform: translateX(171px); opacity: 0; }
}

.fa-bubble:nth-child(22) {
  width: 88px;
  height: 88px;
  left: 42vw;
  animation: faMove22 11s infinite linear;
  animation-delay: -9.3s;
}
@keyframes faMove22 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 3vh; transform: translateX(-95px); opacity: 0; }
}

.fa-bubble:nth-child(23) {
  width: 47px;
  height: 47px;
  left: 32vw;
  animation: faMove23 4s infinite linear;
  animation-delay: -3.2s;
}
@keyframes faMove23 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 53vh; transform: translateX(68px); opacity: 0; }
}

.fa-bubble:nth-child(24) {
  width: 93px;
  height: 93px;
  left: 15vw;
  animation: faMove24 7s infinite linear;
  animation-delay: -0.1s;
}
@keyframes faMove24 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 74vh; transform: translateX(172px); opacity: 0; }
}

.fa-bubble:nth-child(25) {
  width: 75px;
  height: 75px;
  left: 59vw;
  animation: faMove25 12s infinite linear;
  animation-delay: -1.8s;
}
@keyframes faMove25 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 82vh; transform: translateX(194px); opacity: 0; }
}

.fa-bubble:nth-child(26) {
  width: 92px;
  height: 92px;
  left: 46vw;
  animation: faMove26 6s infinite linear;
  animation-delay: -3.3s;
}
@keyframes faMove26 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 47vh; transform: translateX(69px); opacity: 0; }
}

.fa-bubble:nth-child(27) {
  width: 22px;
  height: 22px;
  left: 19vw;
  animation: faMove27 6s infinite linear;
  animation-delay: -3.0s;
}
@keyframes faMove27 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 68vh; transform: translateX(-31px); opacity: 0; }
}

.fa-bubble:nth-child(28) {
  width: 95px;
  height: 95px;
  left: 24vw;
  animation: faMove28 12s infinite linear;
  animation-delay: -0.8s;
}
@keyframes faMove28 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 76vh; transform: translateX(18px); opacity: 0; }
}

.fa-bubble:nth-child(29) {
  width: 39px;
  height: 39px;
  left: 86vw;
  animation: faMove29 8s infinite linear;
  animation-delay: -7.2s;
}
@keyframes faMove29 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 98vh; transform: translateX(71px); opacity: 0; }
}

.fa-bubble:nth-child(30) {
  width: 42px;
  height: 42px;
  left: 4vw;
  animation: faMove30 6s infinite linear;
  animation-delay: -2.8s;
}
@keyframes faMove30 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 56vh; transform: translateX(-100px); opacity: 0; }
}

.fa-bubble:nth-child(31) {
  width: 34px;
  height: 34px;
  left: 62vw;
  animation: faMove31 3s infinite linear;
  animation-delay: -1.7s;
}
@keyframes faMove31 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 52vh; transform: translateX(47px); opacity: 0; }
}

.fa-bubble:nth-child(32) {
  width: 64px;
  height: 64px;
  left: 4vw;
  animation: faMove32 10s infinite linear;
  animation-delay: -9.7s;
}
@keyframes faMove32 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 21vh; transform: translateX(-36px); opacity: 0; }
}

.fa-bubble:nth-child(33) {
  width: 94px;
  height: 94px;
  left: 37vw;
  animation: faMove33 12s infinite linear;
  animation-delay: -1.8s;
}
@keyframes faMove33 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 62vh; transform: translateX(-67px); opacity: 0; }
}

.fa-bubble:nth-child(34) {
  width: 57px;
  height: 57px;
  left: 6vw;
  animation: faMove34 5s infinite linear;
  animation-delay: -1.1s;
}
@keyframes faMove34 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 34vh; transform: translateX(37px); opacity: 0; }
}

.fa-bubble:nth-child(35) {
  width: 91px;
  height: 91px;
  left: 39vw;
  animation: faMove35 11s infinite linear;
  animation-delay: -2.4s;
}
@keyframes faMove35 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 75vh; transform: translateX(60px); opacity: 0; }
}

.fa-bubble:nth-child(36) {
  width: 18px;
  height: 18px;
  left: 77vw;
  animation: faMove36 7s infinite linear;
  animation-delay: -1.3s;
}
@keyframes faMove36 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 31vh; transform: translateX(193px); opacity: 0; }
}

.fa-bubble:nth-child(37) {
  width: 96px;
  height: 96px;
  left: 37vw;
  animation: faMove37 11s infinite linear;
  animation-delay: -1.0s;
}
@keyframes faMove37 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 63vh; transform: translateX(1px); opacity: 0; }
}

.fa-bubble:nth-child(38) {
  width: 92px;
  height: 92px;
  left: 29vw;
  animation: faMove38 9s infinite linear;
  animation-delay: -8.7s;
}
@keyframes faMove38 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 10vh; transform: translateX(110px); opacity: 0; }
}

.fa-bubble:nth-child(39) {
  width: 83px;
  height: 83px;
  left: 98vw;
  animation: faMove39 9s infinite linear;
  animation-delay: -7.9s;
}
@keyframes faMove39 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 81vh; transform: translateX(134px); opacity: 0; }
}

.fa-bubble:nth-child(40) {
  width: 44px;
  height: 44px;
  left: 81vw;
  animation: faMove40 3s infinite linear;
  animation-delay: -2.1s;
}
@keyframes faMove40 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 96vh; transform: translateX(184px); opacity: 0; }
}

.fa-bubble:nth-child(41) {
  width: 9px;
  height: 9px;
  left: 72vw;
  animation: faMove41 14s infinite linear;
  animation-delay: -8.9s;
}
@keyframes faMove41 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 80vh; transform: translateX(148px); opacity: 0; }
}

.fa-bubble:nth-child(42) {
  width: 28px;
  height: 28px;
  left: 3vw;
  animation: faMove42 14s infinite linear;
  animation-delay: -1.1s;
}
@keyframes faMove42 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 71vh; transform: translateX(176px); opacity: 0; }
}

.fa-bubble:nth-child(43) {
  width: 27px;
  height: 27px;
  left: 46vw;
  animation: faMove43 14s infinite linear;
  animation-delay: -10.1s;
}
@keyframes faMove43 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 48vh; transform: translateX(-14px); opacity: 0; }
}

.fa-bubble:nth-child(44) {
  width: 55px;
  height: 55px;
  left: 95vw;
  animation: faMove44 5s infinite linear;
  animation-delay: -2.1s;
}
@keyframes faMove44 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 58vh; transform: translateX(127px); opacity: 0; }
}

.fa-bubble:nth-child(45) {
  width: 94px;
  height: 94px;
  left: 24vw;
  animation: faMove45 14s infinite linear;
  animation-delay: -6.8s;
}
@keyframes faMove45 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 9vh; transform: translateX(-31px); opacity: 0; }
}

.fa-bubble:nth-child(46) {
  width: 30px;
  height: 30px;
  left: 25vw;
  animation: faMove46 14s infinite linear;
  animation-delay: -12.6s;
}
@keyframes faMove46 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 7vh; transform: translateX(-47px); opacity: 0; }
}

.fa-bubble:nth-child(47) {
  width: 84px;
  height: 84px;
  left: 69vw;
  animation: faMove47 5s infinite linear;
  animation-delay: -3.0s;
}
@keyframes faMove47 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 58vh; transform: translateX(-41px); opacity: 0; }
}

.fa-bubble:nth-child(48) {
  width: 67px;
  height: 67px;
  left: 42vw;
  animation: faMove48 11s infinite linear;
  animation-delay: -8.9s;
}
@keyframes faMove48 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 22vh; transform: translateX(54px); opacity: 0; }
}

.fa-bubble:nth-child(49) {
  width: 8px;
  height: 8px;
  left: 50vw;
  animation: faMove49 3s infinite linear;
  animation-delay: -1.0s;
}
@keyframes faMove49 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 10vh; transform: translateX(108px); opacity: 0; }
}

.fa-bubble:nth-child(50) {
  width: 36px;
  height: 36px;
  left: 85vw;
  animation: faMove50 8s infinite linear;
  animation-delay: -3.6s;
}
@keyframes faMove50 {
  0% { bottom: -100px; transform: translateX(0); opacity: 0; }
  10% { opacity: 0.8; }
  100% { bottom: 71vh; transform: translateX(66px); opacity: 0; }
}